import { MobileSection } from "@/components/mobile/shell/MobileSection";

export default function MobileActionPage() {
  return (
    <MobileSection
      eyebrow="Aksi"
      title="Primary Action"
      description="Tombol aksi kontekstual seperti clock in, order, atau KDS queue akan diletakkan di sini."
    />
  );
}
